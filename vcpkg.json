{
  "name": "pattern",
  "version-string": "0.1.1",
  "description": "Dependencies for building the Pattern FFGL plugin. GLEW is only needed on Windows; macOS uses the system OpenGL framework. There is nothing else: the tracker is arithmetic, the font is a table in Font.cpp, and the only audio that reaches the plugin is the host's own FFT buffer.",
  "dependencies": [
    {
      "name": "glew",
      "platform": "!osx"
    }
  ]
}
